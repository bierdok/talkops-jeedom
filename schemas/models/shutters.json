{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "id": {
        "type": "integer",
        "description": "The shutter unique identifier"
      },
      "name": {
        "type": "string",
        "description": "The shutter name"
      },
      "state": {
        "type": "string",
        "enum": ["opened", "unknown", "closed"],
        "description": "The shutter state"
      },
      "location_id": {
        "type": "string",
        "description": "The related location identifier"
      }
    },
    "required": ["id", "name", "state"]
  }
}